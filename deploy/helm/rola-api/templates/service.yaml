apiVersion: v1
kind: Service
metadata:
  name: {{ include "rola-api.fullname" . }}
  labels:
    {{- include "rola-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rola-api.selectorLabels" . | nindent 4 }}
