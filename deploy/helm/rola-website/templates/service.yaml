apiVersion: v1
kind: Service
metadata:
  name: {{ include "rola-website.fullname" . }}
  labels:
    {{- include "rola-website.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rola-website.selectorLabels" . | nindent 4 }}
